spring:
  application:
    name: travel-web
  datasource:
    url: jdbc:mysql://localhost:3306/travel?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&connectTimeout=10000&socketTimeout=30000
    username: root
    password: 19990421Hzhf
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 300000
      connection-timeout: 20000
      max-lifetime: 1200000
  jpa:
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl  # ← 移到这里
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  sql:
    init:
      mode: never
  thymeleaf:
    cache: false

server:
  port: 8080
  servlet:
    session:
      cookie:
        name: TRAVELSESSION

logging:
  level:
    org.hibernate.SQL: warn
    com.travel.travelweb: info